---
# --- YAML Frontmatter ---
created: 2026-02-23 09:22
modified: 2026-02-23 09:52
environment: [OS/Linux, OS/Unix, OS/Windows, Networking]
vulnearability: [ICMP_Flooding, Reconnaissance]
knowledge_category: Command
tags:
  - ping
  - networking
  - troubleshooting
  - icmp
  - coreutils
  - knowledge_base
---

# Command - Linux - ping - ホストへのネットワーク疎通を確認する

## 概要

`ping` は ICMP (Internet Control Message Protocol) の **ECHO_REQUEST** パケットを対象のホストに送信し、**ECHO_REPLY** が返ってくるまでの時間（RTT: Round-Trip Time）と、パケットが途中で失われていないか（パケットロス率）を計測します。

## 🛠 ネットワーク診断ツールの比較

| コマンド | 役割 | 特徴 |
| :--- | :--- | :--- |
| **`ping`** | 基本的な疎通確認 | 単一ホストの死活監視に最適。 |
| `fping` | 複数ホストへの一括 ping | スクリプトで大量のサーバーを一度にチェック。 |
| `hping3` | 高度なパケット生成 | TCP/UDP を使ったカスタムパケット送信（攻撃にも防御にも）。 |
| `mtr` | 経路調査 + ping | 各ホップごとの品質をリアルタイム表示。 |

## 主要なオプション

※Linux の `ping` は、Windows と違い放っておくと **永遠に止まりません。** 停止するには `Ctrl + C` を押します。

| オプション | 説明 |
| :--- | :--- |
| ⭐️ `-c <count>` | 送信回数を指定して終了する。 |
| ⭐️ `-i <interval>` | パケット送信の間隔（秒）を指定（デフォルトは 1 秒）。 |
| ⭐️ `-s <size>` | 送信するパケットのデータサイズ（バイト）を指定。 |
| ⭐️ `-t <ttl>` | 生存時間 (Time To Live) を指定。 |
| ⭐️ `-f` (Flood) | **管理者のみ。** 可能な限り高速にパケットを投げまくる（負荷テスト用）。 |
| `-a` | 応答があった時にビープ音を鳴らす（「生きた！」瞬間に気づける）。 |

## コマンド例

```bash
# 4回だけパケットを送り、応答を確認
ping -c 4 google.com

# 0.2秒間隔で高速にチェック（サーバー起動直後の確認などに）
sudo ping -i 0.2 192.168.1.1
````

---

## 📊 結果の読み方

- **icmp_seq**: パケットの通し番号。抜けがあるとパケットロスが発生しています。
- **ttl**: ルーターを通過できる上限数。OSの推測にも使われます。
- **time**: 応答時間（ミリ秒）。数値が大きく、かつ変動が激しい場合はネットワークが不安定です。

---

## 業務シナリオ別活用法

### 1. インフラ構築・運用視点

- **タスク**: サーバー起動後のネットワーク設定確認。
- **解説**: 物理配線や IP 設定、デフォルトゲートウェイが正しいか、内側から外側へ順番に `ping` を打って切り分けます。

### 2. ブルーチーム視点

- **タスク**: ICMP フラッド攻撃の検知と緩和。
- **解説**: 短時間に大量の ICMP パケットが届く「Ping of Death」や「ICMP Flood」による負荷を監視し、ファイアウォールで制限をかける判断材料にします。

### 3. レッドチーム視点

- **タスク**: ネットワーク内の生存ホスト探索（Host Discovery）。
- **解説**: `fping` や `nmap -sn` と同様に、どの IP アドレスでマシンが稼働しているかを特定します。また、`ping` の TTL 値を確認することで、ターゲットの OS（Linux なら 64 付近、Windows なら 128 付近）を推測します。

####  高速 Host Discovery ワンライナー

このコマンドは、Cクラス（/24）のネットワーク全体（1〜254）に対して同時に ping を投げ、応答があった IP だけを表示します。

Bash

```
for i in {1..254}; do (ping -c 1 -W 1 192.168.1.$i > /dev/null && echo "192.168.1.$i is UP" &); done; wait
```

##### コマンドの徹底解剖

- **`for i in {1..254}; do ... done`**: IPアドレスの末尾を 1 から 254 まで変化させてループ回します。
- **`( ... &)`**: カッコ内の処理を**サブシェルとしてバックグラウンドで実行**します。これにより、254台への ping をほぼ同時に開始できるため、スキャンが数秒で終わります。
- **`ping -c 1 -W 1`**:
    - `-c 1`: パケットを 1 つだけ送信。
    - `-W 1`: 応答を待つタイムアウトを 1 秒に制限（これがないと不在ホストで長く待たされます）。
- **`> /dev/null`**: ping の詳細な出力（bytes from... など）を捨てます。
- **`&& echo "... is UP"`**: ping が成功した場合（終了ステータス 0）のみ、IP アドレスを表示します。
- **`wait`**: すべてのバックグラウンドプロセス（254個のping）が終了するまで待機してからプロンプトを戻します。

---

#### 💡 シナリオ別の微調整

##### 1. 特定の IP リストに絞る（より静かに）

特定のサーバー群だけを確認したい場合は、ブレース展開を調整します。

Bash

```
for i in {10,20,50,100}; do (ping -c 1 -W 1 192.168.1.$i > /dev/null && echo "192.168.1.$i is UP" &); done
```

##### 2. 結果をファイルに保存する

後で解析するために、生存リストをテキストファイルへ書き出します。

Bash

```
for i in {1..254}; do (ping -c 1 -W 1 192.168.1.$i > /dev/null && echo "192.168.1.$i" >> live_hosts.txt &); done; wait
```

---

#### ⚠️ レッドチーム／ブルーチームへのアドバイス

- **レッドチーム視点（ステルス性）**:

    このワンライナーは非常に「騒がしい」です。短時間に 254 発の ICMP パケットが飛ぶため、IDS/IPS（侵入検知システム）に「ICMP スキャン」として即座に検知される可能性が高いです。検知を避けたい場合は、`&` を外してゆっくり実行するか、`-i` で間隔をあける必要があります。

- **ブルーチーム視点（防御）**:

    多くのサーバーはセキュリティのために `ping` 応答を無効化（Drop）しています。このワンライナーで「反応がない」からといって「そこにマシンがいない」とは限りません。真の Host Discovery を行うには、`arp-scan` や `nmap` による ARP/TCP スキャンを併用するのが 2026 年の定石です。

---

## セキュリティに関する考慮事項

### 脆弱性とリスク

- **情報漏洩 (Reconnaissance)**: `ping` を許可していると、攻撃者に「そこにサーバーがあること」がバレてしまいます。
- **ICMP トンネリング**: 高度な攻撃者は、`ping` パケットのデータ領域に情報を隠して通信を行い、ファイアウォールを潜り抜けてデータを持ち出すことがあります。

### 対応策・緩和策 (ブルーチーム視点)

- **選択的な許可**: 公開サーバーでは、不必要な ICMP 応答を無効化（`echo 1 > /proc/sys/net/ipv4/icmp_echo_ignore_all`）するか、信頼できる監視サーバーからのみ許可するように設定します。

