OSCP合格に向けて、これまでの対話で練り上げた「効率・記録・直感」を重視した最強のKali Linux構成マスタガイドです。

新しい環境を作る際は、この内容を上から順番に実行・設定してください。

---

## 1. システム基盤とOS設定

長時間スキャンや大容量ログに耐えうる足回りを作ります。

* **仮想化:** VMware Workstation (公式推奨)
* **ターミナル:** **Kitty**（GPU加速・画像表示 `icat` 対応）
```bash
sudo apt update && sudo apt install -y kitty fonts-jetbrains-mono
sudo update-alternatives --config x-terminal-emulator # Kittyを選択

```


* **スリープ・ロック回避:**
```bash
gsettings set org.gnome.desktop.screensaver lock-enabled false
gsettings set org.gnome.desktop.session idle-delay 0
xset s off
xset -dpms

```



---

## 2. ディレクトリ構造の整理

「どこに何があるか」を迷わないための厳格なルールです。

```bash
mkdir -p ~/oscp/{labs,exam,scripts,logs}
mkdir -p ~/tools/git

```

* `/opt`: システムツール（インストールが必要なもの）
* `~/tools/git`: GitHubからクローンしたスクリプト（`gclone`でここへ）
* `~/oscp/scripts`: ターゲット転送用の厳選ツール（LinPeas等）
* `~/oscp/logs`: Tmuxの自動ログ保存先

---

## 3. 設定ファイルの完全版

### ① `~/.config/kitty/kitty.conf` (視認性重視)

```conf
# --- フォント設定 ---
# 視認性抜群の JetBrains Mono か Fira Code の Nerd Font 版がおすすめ
font_family      JetBrainsMono Nerd Font
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size        12.0

# --- スクロールバック (OSCPでは命) ---
# ログが消えないよう多めに設定。記憶力が足りなくなったらファイルへ
scrollback_lines 20000

# --- ベル設定 (集中力を削がない) ---
enable_audio_bell no
visual_bell_duration 0.0

# --- ウィンドウ・デザイン ---
# わずかな透過は、裏の資料をチラ見するのに便利
background_opacity 0.92
# ウィンドウの余白（少しあると見やすい）
window_padding_width 4

# --- コピペの挙動 ---
# 選択するだけでコピー（Puttyスタイル。慣れると爆速です）
copy_on_select yes
# クリップボードとの同期
strip_trailing_spaces smart

# --- パフォーマンス ---
# 遅延を最小限に
repaint_delay 10
input_delay 3
sync_to_monitor yes

# --- 色設定 (Theme) ---
# 結局「Gruvbox Dark」か「Nord」が一番目が疲れません
# ここでは目に優しい Gruvbox 系の色味を推奨
foreground            #ebdbb2
background            #282828
selection_foreground  #282828
selection_background  #ebdbb2

```

### ② `~/.tmux.conf` (直感操作と自動記録)

```bash
# プレフィックスを Ctrl+a に
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# 【直感分割】 | で縦分割、 - で横分割
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

set -g mouse on
set -g history-limit 10000

# プラグイン設定 (TPMが必要)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-logging'
set -g @logging-path "~/oscp/logs"

run '~/.tmux/plugins/tpm/tpm'

```

※初回起動時に `Prefix + I` でプラグインをインストール。

### ③ `~/.zshrc` (自動化と高速化)

```bash
# ツール初期化
eval "$(fzf --zsh)"
eval "$(zoxide init zsh --cmd z)"
source /usr/share/zsh-autosuggestions/zsh-autosuggestions.zsh
source /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

# ターゲット管理関数
target() {
    export ip="$1" target="$1"
    local tag="# OSCP_TARGET"
    if [ -n "$2" ]; then
        export fqdn="$2"
        sudo sed -i "/ $fqdn/d; /^$ip /d" /etc/hosts
        echo "$ip $fqdn $tag" | sudo tee -a /etc/hosts > /dev/null
    fi
    echo "[+] Target: $ip ($fqdn)"
    getent hosts "${fqdn:-$ip}"
}

target_clear() {
    sudo sed -i "/# OSCP_TARGET/d" /etc/hosts
    unset ip target fqdn
    echo "[!] Environment & Hosts cleared."
}

# エイリアス
alias gclone='cd ~/tools/git && git clone'
alias update-tools='find ~/tools/git -maxdepth 2 -name .git -type d -execdir git pull \;'
alias pserv="python3 -m http.server 80"
alias udot="updog -p 80"
alias icat="kitty +kitten icat"

```

---

## 4. Python ツール管理 (`pipx`)

```bash
sudo apt install -y pipx && pipx ensurepath
# 重要ツール
pipx install git+https://github.com/Tib3rius/AutoRecon.git
pipx install impacket
pipx install netexec
pipx install updog

```

---

## 5. 構築完了チェックリスト

* [ ] **Kitty:** `icat` で画像が表示され、背景が適度に透過しているか？
* [ ] **Tmux:** `|` と `-` でペインを割れるか？ `Prefix + Shift + p` でログが `~/oscp/logs` に保存されるか？
* [ ] **zoxide/fzf:** `z <dir>` で移動でき、`Ctrl + R` で履歴を曖昧検索できるか？
* [ ] **target関数:** `target <IP> <FQDN>` で `/etc/hosts` に `# OSCP_TARGET` 付きで追記されるか？
* [ ] **メンテナンス:** `update-tools` を実行して `~/tools/git` 内の更新が走るか？
* [ ] **スクリーンショット:** `Flameshot` が `PrintScreen` キーで起動するか？

**【最重要】すべての設定を終えた状態で「クリーンなスナップショット」を撮影してください。**

これで、あなたのOSCP攻略環境は完成です。この環境は、どのマシンに移行しても同じ手順で再現でき、試験本番のストレスを最小限に抑えてくれます。さあ、次はどのターゲットを攻略しますか？