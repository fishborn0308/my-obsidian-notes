以下に、ここまで構築してきた環境を**運用単位で整理**します。冗長さを排し、再現可能な形にまとめています。

---

## 全体アーキテクチャ

```text
編集系（Mac / Windows）
→ Cursor / Obsidian / Git

実行系（Kali）
→ tmux / scan / enum / exploit

同期
→ Syncthing（データのみ）
```

---

## 設計思想（中核）

```text
設定は分離
データは共有
実行はKali
```

---

## ディレクトリ構成（Kali）

```text
~/Vault/Target/<IP>/
├── assets/
├── result/
│   ├── nmap_*
│   └── autorecon/
├── log/
├── <IP>.md
└── workbench -> ~/Workbench
```

---

## Workbench

```text
~/Workbench/
├── Recon/
├── Web/
├── AD/
├── Exploit/
├── Wordlists/
├── Scripts/
├── Tools -> ~/Tools
└── Transfer -> ~/Transfer
```

---

## セッション設計（tmux）

### セッション分離

```text
1 target = 1 session
```

例：

```text
meow_187
legacy_20
t_46_187
```

---

### ペイン構成

```text
[左]   scan   → target dir
[右上] enum   → ~/Workbench
[右下] notes  → target dir
```

---

### infra セッション（別）

```text
infra
├── VPN
├── HTTP server (pserv / updog)
├── pivot (ligolo / chisel)
└── Transfer作業
```

---

## フロー（実務）

```text
targettmux <IP> <name>
↓
scaninit
↓
ports-init / next / nextsvc
↓
手動列挙
↓
（並行）
fullcheck
↓
詰まったら
→ autorecon（手動）
→ UDP（必要時）
```

---

## スキャン設計

## 初動（scaninit）

```text
Rustscan
→ fallback: top1000
→ fallback: full scan
```

### サービススキャン

```text
nmap -sC -sV -p <ports>
```

### フルスキャン

```text
バックグラウンド実行
```

---

## 補助関数

### 重要

```text
target
targetcl
scaninit
ports-init / ports-serv
next / nextsvc
fullcheck
```

---

### 非自動（意図的）

```text
autorecon → 手動（TTY必要）
UDP       → 手動（ケース依存）
```

---

## nextsvc の改善ポイント

```text
grep対象 = openポート行のみ
```

👉 誤検知防止（https問題解決）

---

## fullcheck

```text
実行中 → log表示
完了後 → openポート + 差分
```

---

## ログ戦略

```text
tmux-logging
→ 手動開始（現段階）
```

---

## 設定管理

### バックアップ

```zsh
buckconf
```

### 復元

```zsh
restoreconf
```

---

### config管理

```text
~/Vault/Cursor/40_Configs/
```

---

## シンボリックリンク戦略

```text
target → workbench
workbench → Tools / Transfer
```

👉 二重リンクは作らない（冗長回避）

---

# 自動化レベル（重要）

## 自動化済み

```text
scaninit
tmux session分離
3ペイン構成
ports抽出
```

## 半自動

```text
ログ開始
```

## 手動

```text
autorecon
UDP
exploit判断
```

---

# 重要な設計判断まとめ

## ① tmux分離

```text
targetごとに完全独立
```

## ② Workbench導入

```text
作業導線の統一
```

## ③ 自動化しすぎない

```text
補助系は手動維持
```

## ④ データと設定分離

```text
Obsidian設定はKaliと分離
```

---

# 現在の完成度

```text
・環境設計 → 完成
・スキャンフロー → 完成
・思考補助 → 完成
・tmux運用 → 完成
・拡張余地 → 残しつつ安定
```

---

# 最終結論

```text
実戦投入可能なペンテスト作業環境
```

---

必要なら次は：

```text
・scaninitの最終最適化
・nextsvcのサービス網羅拡張
・Obsidianテンプレの自動連携
```

あたりが次フェーズです。