---
tags:
  - history
  - shell
  - forensics
  - bash
  - grep
  - cat
created: 2025-06-29 15:02
modified: 2026-02-20 15:39
environment: [OS/Linux]
vulnearability: []
knowledge_category: Command
---

# Command  - Linux - history - コマンド実行履歴を表示・操作する

## 概要

`history` は、現在のシェルセッションで過去に実行したコマンドの履歴を一覧表示するためのシェル組み込みコマンドです。ユーザーが実行したコマンドは、セッション中はメモリに保持され、シェル終了時に履歴ファイル（通常は `~/.bash_history`）に書き込まれます。`history` は、過去の複雑なコマンドを再実行したり、過去の操作を振り返ったりするための便利な機能であると同時に、システム上で行われた操作の重要な記録でもあります。

(出自: `bash`, `zsh` などのシェルの組み込み機能)

## 類似コマンドと差異

| 類似コマンド | 差異 / 使い分けのポイント | 推奨 |
| :--- | :--- |:---|
| **`Ctrl + R`** | **後方検索モード**。キーワードを入力すると、過去のコマンド履歴から一致するものを対話的に検索・表示できます。履歴全体を見る`history`に対し、特定のコマンドを探すのに非常に高速です。 | `Ctrl + R` (検索) |
| **`cat ~/.bash_history`**| 履歴ファイルを直接表示します。`history`コマンドはメモリ上の現在のセッション履歴も表示しますが、`cat`はファイルに保存された過去のセッション履歴のみを表示します。 | - |

## よく連携されるコマンド

`history` の出力はパイプ (`|`) を通じて他のコマンドに渡すことができ、`grep` との連携は非常に強力です。

### シナリオ例: 過去の特定のコマンドを探し出す (インフラ構築・運用視点)

* **目的**: 以前実行した、複雑なオプションを持つ `rsync` コマンドをもう一度使いたいが、正確に思い出せない。
* **連携コマンド**: `grep`
* **解説**: `history` で全履歴を出力し、その結果をパイプで `grep` に渡してキーワードで絞り込みます。これにより、目的のコマンドを簡単に見つけ出すことができます。
* **コマンド例**:

```bash
# 履歴の中から "rsync" を含むコマンドを検索する
history | grep 'rsync'
```

## `history` の主要な機能とオプション

| オプション/機能 | 説明 |
| :--- | :--- |
| **表示と操作** | |
| ⭐ `history` | 全ての履歴を番号付きで表示します。 |
| `history <N>` | 直近 `N` 件の履歴を表示します。 |
| `-c` | 現在のシェルセッションの履歴（メモリ上）を全て消去します。 |
| `-d <N>` | 履歴番号 `N` のコマンドを削除します。 |
| **履歴の再実行 (Expansion)** | |
| ⭐ `!!` | **直前**に実行したコマンドを再実行します。`sudo !!` は非常に頻繁に使われます。 |
| ⭐ `!<N>` | 履歴番号 `N` のコマンドを再実行します。（例: `!101`） |
| `!<string>` | `string` で始まる**直近の**コマンドを再実行します。（例: `!docker`） |
| **履歴の検索** | |
| ⭐ **`Ctrl + R`** | **後方検索モード**を開始します。キーワードを入力すると、履歴から一致するものを対話的に検索できます。 |

## よく使われるオプション組み合わせと業務シナリオ

### 1. インフラ構築・運用視点

* **タスク**: `sudo` を付け忘れた直前のコマンドを再実行する。
* **組み合わせ**: `sudo !!`
* **解説**: `apt install nginx` のようなコマンドが権限不足で失敗した場合、`sudo !!` と入力するだけで `sudo apt install nginx` として再実行できます。これは管理業務を大幅に効率化する黄金パターンです。
* **例**:

```bash
apt install nginx
# -> permission denied...
sudo !!
# -> sudo apt install nginx が実行される
```

### 2. ブルーチーム視点

* **タスク**: 侵害された疑いのあるユーザーの活動内容を調査する。
* **組み合わせ**: `cat ~/.bash_history`
* **解説**: **フォレンジック調査の最重要項目の一つ**。`user1` のアカウントが侵害された疑いがある場合、`/home/user1/.bash_history` を確認します。`wget http://.../evil.sh` や `nc -l -p ...` のようなコマンドが残っていれば、攻撃者が偵察、ペイロードのダウンロード、バックドアの設置を行った明確な証拠となります。
* **例**:

```bash
# 侵害されたユーザーのコマンド履歴を確認
sudo cat /home/compromised_user/.bash_history
```

### 3. レッドチーム視点

* **タスク**: 侵入後の情報収集と、自身の活動の痕跡消去。
* **組み合わせ**: `cat ~/.bash_history`, `history -c`
* **解説**:
	* **偵察**: 攻撃者は侵入後、まず `.bash_history` を確認し管理者が普段どのようなコマンドを実行しているか、システムの構成、さらには `mysql -u user -p'password'` のように誤って入力されたパスワードなどの機密情報を窃取します。
	* **痕跡消去**: 活動終了後、`history -c && rm ~/.bash_history` のようにして、現在のセッション履歴と履歴ファイルの両方を削除し、追跡を困難にします。
* **例**:

```bash
# 履歴ファイルからパスワードやキーの文字列を探す
cat ~/.bash_history | grep -i "password\|key\|token"
```

## エラーメッセージとトラブルシューティング

* 一般的なエラーは [Linux共通のトラブルシューティング](./troubleshooting_common_errors.md) を参照。
* `history` はシェル組み込みコマンドのため、特有のエラーはほとんどありません。

## 環境変数と設定ファイル

* 一般的な環境変数・設定ファイルは [Linux共通の環境変数・設定ファイル](./environment_and_config.md) を参照。

### 影響を与える環境変数

* **`HISTSIZE`**: メモリ上に保持する履歴のコマンド数。
* **`HISTFILESIZE`**: 履歴ファイル (`~/.bash_history`) に保存するコマンド数。
* **`HISTCONTROL`**: 履歴の保存方法を制御します。`ignorespace`: 先頭がスペースのコマンドを保存しない。`ignoredups`: 重複するコマンドを保存しない。
* ⭐ **`HISTTIMEFORMAT`**: `history` の出力にタイムスタンプを付与します。例: `export HISTTIMEFORMAT="%F %T "`

## セキュリティに関する考慮事項

### 脆弱性と悪用事例

* **脆弱性**: `history` 自体ではなく、**履歴ファイルに保存される内容**がセキュリティリスクとなります。
* **悪用シナリオ**: **機密情報の漏洩**。コマンドライン引数にパスワードやAPIキーを直接入力すると、`.bash_history` に平文で保存され、ファイルへのアクセス権を持つ誰からでも閲覧可能になってしまいます。これは最もよくある情報漏洩の原因の1つです。

### GTFOBins / LOLBAS における利用例

`history` は情報収集に使われますが、直接的な権限昇格機能はないためGTFOBinsにはリストされていません。

### 対応策・緩和策 (ブルーチーム視点)

* **Prevention (予防)**: パスワードなどの機密情報をコマンドライン引数で渡さない。シェルの設定 (`HISTCONTROL=ignorespace`) を活用し、機密情報を扱うコマンドの先頭にスペースを入れて履歴に残さないようにする運用を徹底する。
* **Detection (検知)**: ユーザーの `.bash_history` ファイルを定期的に監査し、不審なコマンド実行がないか確認する。`auditd` でコマンド実行をより詳細に記録する。

## 注意点・補足

* **タイムスタンプの追加**: 監査やインシデント追跡のために、いつコマンドが実行されたかの情報は非常に重要です。`~/.bashrc` に `export HISTTIMEFORMAT="%F %T "` を追記しておくと、`history` の出力にタイムスタンプが付与され非常に便利です。

